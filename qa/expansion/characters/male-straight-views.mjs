import v from './cast-views.mjs';const steps=[v[0]];for(const id of ['jules','noah'])for(const [pose,angle,state]of [['front',0,{}],['side',Math.PI/2,{}],['walk',0,{walking:true,walkDistance:.25}],['sit',.5,{sitting:true}]])steps.push({name:id+'-'+pose,run:`for(const a of Object.values(qaCast))a.group.visible=false;qaShow('${id}',-2,1,${angle},${JSON.stringify(state)});qaRender(-2,1.05,3.5,-2,.88,1,42);`});export default steps;
