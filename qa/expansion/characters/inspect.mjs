import fs from 'node:fs';let b=fs.readFileSync('assets/characters/noah.glb'),n=b.readUInt32LE(12),j=JSON.parse(b.subarray(20,20+n)),bin=b.subarray(n+28);for(const m of j.meshes){console.log(m.name,m.extras?.targetNames);for(const p of m.primitives){let a=j.accessors[p.attributes.POSITION],v=j.bufferViews[a.bufferView],o=(v.byteOffset||0)+(a.byteOffset||0);console.log(j.materials[p.material].name,a.count,a.min,a.max,p.targets?.length);}}