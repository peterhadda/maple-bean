import views from './views.mjs';
export default [...views,{name:'material-and-light-check',shot:false,run:`const materials=new Set();const shadowLights=[];cafe.scene.traverse(o=>{if(o.isLight&&o.castShadow)shadowLights.push(o);for(const m of (Array.isArray(o.material)?o.material:[o.material]))if(m)materials.add(m);});const wood=[...materials].filter(m=>/oak|plank/i.test(m.name));const finish=[...materials].find(m=>m.name==='Bean fluted pine joinery');const glow=[...materials].find(m=>m.name==='Bean warm light shelf inset');if(!finish||!glow)throw Error('Bar refinement missing');if(!wood.some(m=>m.roughness===.48&&m.bumpScale===.003)||!wood.some(m=>m.roughness===.76))throw Error('Wood material hierarchy missing');if(shadowLights.length!==1)throw Error('Unexpected additional shadow map');if([...materials].some(m=>!Number.isFinite(m.opacity)))throw Error('Invalid material');return {woodFinishes:wood.length,shadowLights:shadowLights.length,shelfGlow:glow.emissiveIntensity};`}];
